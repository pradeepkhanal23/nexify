import { ShoppingCart } from "lucide-react";
import { Button } from "../ui/button";

const AddToCart = () => {
  return (
    <Button>
      <ShoppingCart />
    </Button>
  );
};
export default AddToCart;
